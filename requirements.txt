fastapi
uvicorn
python-jose[cryptography]
passlib[bcrypt]
python-multipart
transformers
torch
sentence-transformers
Pillow
opencv-python
moviepy
# SpeechRecognition # Not actively used yet, can be commented out or kept
# pydub # Not actively used yet
pdfminer.six
python-docx
# requests # Not actively used by current backend logic, consider removing if not planned soon
# beautifulsoup4 # Not actively used
# tineye-api # Not actively used
# google-api-python-client # Not actively used

# Database
sqlalchemy
alembic
python-dotenv # For loading .env files for config, including DATABASE_URL

# AI Services
openai
stripe

# Note: For SQLite, no separate driver needed in requirements.txt.
# For PostgreSQL, add: psycopg2-binary
